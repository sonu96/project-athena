# Production Environment Configuration Template
# =====================================================
# IMPORTANT: This is a template file. DO NOT add actual secrets here!
# 
# For local development: Copy this to .env.production.local and fill in values
# For Cloud Run: These values are set via Secret Manager and environment variables
#
# To set up your deployment, run:
# 1. ./scripts/setup_gcp.sh      - Set up GCP project
# 2. ./scripts/setup_github_secrets.sh - Configure GitHub secrets
# 3. ./scripts/setup_secrets.sh  - Set up Secret Manager
# =====================================================

# Core APIs (Stored in GCP Secret Manager)
# These are referenced in Cloud Run deployment but need actual values locally
OPENAI_API_KEY=your_openai_api_key_here
MEM0_API_KEY=your_mem0_api_key_here

# Base Blockchain Configuration
BASE_RPC_URL=https://mainnet.base.org
# Private key is stored in Secret Manager for production
AGENT_PRIVATE_KEY=your_agent_private_key_here
# Your agent's wallet address (public)
AGENT_ADDRESS=your_agent_wallet_address_here

# GCP Configuration
GCP_PROJECT_ID=project-athena-personal
BIGQUERY_DATASET=athena_analytics
FIRESTORE_COLLECTION=agent_memories
GCS_BUCKET_NAME=project-athena-personal-storage

# Agent Configuration
INITIAL_TREASURY=1000.0
MODE=PERSONAL
AGENT_NAME=PersonalDeFiAgent

# Cost Controls
MAX_DAILY_SPEND=10.0
MAX_TRANSACTION_SIZE=100.0
MIN_TREASURY_BALANCE=50.0

# Optional Services (uncomment if using)
# REDIS_URL=redis://10.0.0.3:6379
# DATABASE_URL=postgresql://user:password@/dbname?host=/cloudsql/project:region:instance

# Monitoring Configuration
ENABLE_MONITORING=true
LOG_LEVEL=INFO
ALERT_EMAIL=your_email@example.com

# Security Configuration
ENABLE_RATE_LIMITING=true
MAX_REQUESTS_PER_MINUTE=60
ALLOWED_ORIGINS=https://your-frontend-domain.com

# Automation Settings
ENABLE_SCHEDULED_TASKS=true
TREASURY_CHECK_HOUR=9
MARKET_SCAN_INTERVAL_HOURS=4
MEMORY_OPTIMIZATION_DAY=0  # 0=Monday, 6=Sunday

# Performance Settings
MEMORY_CACHE_TTL=300  # 5 minutes
MAX_MEMORY_SEARCH_RESULTS=10
DECISION_TIMEOUT_SECONDS=30